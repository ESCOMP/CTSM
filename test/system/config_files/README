vo    => voc                    is on
ds    => dust                   is on
r     => rtm                    is on
per   => pergro                 is on
sc    => seq-ccsm               is on
urb   => urban_solar 	        is on
van   => VANCOUVER              is on
mex   => MEXICOCITY             is on

_cnn  => bgc is in cn   mode with suplemental nitrogen on
_cn   => bgc is in cn   mode with suplemental nitrogen off
_casa => bgc is in casa mode
_dgvm => bgc is in dgvm mode

_dh   => debug on, hybrid   on
_dm   => debug on, mpi only on 
_do   => debug on, omp only on
_do   => debug on, serial mode (neither mpi nor omp)

_h    => debug off, hybrid   on
_m    => debug off, mpi only on
_o    => debug off, omp only on
_s    => debug off, serial modeserial mode (neither mpi nor omp)

tools__ds => options for tools, debug on, serial mode
tools__o  => options for tools, debug off, omp only on

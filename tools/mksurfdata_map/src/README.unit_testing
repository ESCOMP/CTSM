# To run all unit tests defined here, run the following command:
#
# From a standalone CTSM checkout:

../../../cime/scripts/fortran_unit_testing/run_tests.py --build-dir unit_test_build

# If you are within a full CESM checkout, you would instead do:

../../../../../cime/scripts/fortran_unit_testing/run_tests.py --build-dir unit_test_build

This test sets soil_layerstruct_userdefined to the dzsoi values of the
predefined case 4SL_2m and checks for bfb same answers.

This is done by
(a) listing the test in (1) testlist_clm.xml (as all tests) and
                        (2) config_tests.xml
(b) creating the file .../cime_config/SystemTests/soilstructud.py
named after the test in lower case

NB: This test is testing the 10x15 resolution only.
